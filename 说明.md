为什么这套“组合拳”能解决卡顿？
数据库索引 (db_index=True)：
就像是一本 6 万页的字典。没有索引时，你要找 2025 年的订单，数据库得从第 1 页翻到第 60000 页（全表扫描）。加上索引后，数据库直接翻开“目录”，0.01秒就能找到目标数据。

真正的服务端分页 (Paginator)：
如果你的前端视图试图一次性画出 6 万行表格，任何顶级电脑的浏览器都会卡死。使用 Django 自带的 Paginator 后，系统每次只从数据库拿 20 条数据给前端，无论你库里有 6 万条还是 600 万条，速度永远是一样的。

字段级查询 (.only())：
如果你只看订单号和金额，就不需要让数据库把长篇大论的“订单备注”也一起读出来丢进内存里。

1. 分页总数缓存 (Count Cache)
传统的 Paginator 在你每次点“下一页”时，都会问数据库：“喂，总共有多少条？” 数据库就要把 6 万条数据数一遍。
优化后：数据库只在第一次被问到时数一遍，然后把结果记在内存（缓存）里。接下来的 5 分钟内，翻页操作将不再触发 COUNT 查询，响应时间能从几百毫秒降到几十毫秒。

2. 查询参数保留 (Query Parameter Persistence)
这是很多新手开发的通病：用户搜了“现金支付”的订单，看到第一页有 20 条，点“下一页”，结果系统跳回了“所有支付方式”的第二页。
优化后：我们通过在分页链接中动态注入当前 URL 的参数，确保用户的搜索上下文在翻页过程中始终“粘”在 URL 上。